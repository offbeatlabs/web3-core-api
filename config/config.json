{
  "sqlite_config": {
    "path": "${SQLITE_DB_PATH}"
  },
  "helper_flags": {
    "run_migrations": "${RUN_MIGRATIONS_FLAG}"
  },
  "feature_flags": {
    "enable_token_sync": "${ENABLE_TOKEN_SYNC_FLAG}",
    "enable_price_sync": "${ENABLE_PRICE_SYNC_FLAG}"
  },
  "server_config": {
    "base_url_for_swagger": "${BASE_URL_FOR_SWAGGER}"
  }
}